{
  "name": "xeroTechnicalChallenge",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "jest-mock-extended": "^1.0.13",
    "nodemon": "^2.0.7",
    "rimraf": "^3.0.2"
  },
  "scripts": {
    "start": "yarn run build && node dist/index.js",
    "start:dev": "nodemon",
    "test": "jest",
    "build": "rimraf ./dist && tsc"
  },
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "@types/node": "^14.14.37",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.4",
    "typescript": "^4.2.4"
  }
}
